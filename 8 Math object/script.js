// Math  = built-in object that provides a collection of properties and methods

// console.log(Math.PI);


let x = 45;
let y = 4.2;

// z = Math.round(x);   //6
// z = Math.floor(x);  // always rounds down    //6
// z = Math.ceil(x);  //always rounds up  //7
// z = Math.trunc(x);   //6
// z = Math.pow(x,y); 
// z = Math.sqrt(x);
// z = Math.log(x);
// z = Math.sin(x); 
// z = Math.cos(x); 
// z = Math.tan(x); 
// z = Math.abs(x);  //it gives +ve when the variable is neg
// z = Math.sign(x);     //describes the sign of the number  -1 or 1 or 0

// z = Math.max(x,y);
z = Math.min(x,y); 
console.log(z);

